def calcPow(a, b)
    sum = 1;
    i = 0;
    loop do
        if(i == b)
            break;
        end
        sum = sum * a;
        ++i;
    end
    return sum;
end

def main() 
    puts(calcPow(3, 5));
end